# Ripple 协作约定

## 工作目录与数据边界

开始工作前阅读 README.md、相关源码和测试，并核对当前分支、未提交改动及远端状态。这是 Ripple 的产品代码工作目录；仓库是否连接远端或公开，由发布流程单独决定。

正式源码、测试、合成样本、必要资源、依赖锁文件、构建配置及现行产品文档放在这里。私人过程材料放在 `../ripple-work/`；真实项目、音频、转录稿和凭据只留在本机，绝不加入任何 Git 仓库。不要把忽略规则当作已提交内容的保护。

旧的 `transcription-editor` 只作为归档历史，不再作为日常开发目录。日常开发、debug 和发布只使用 `ripple-public`；真实数据只使用 `ripple-local`。

## 分支、提交与 main

- 不直接在 `main` 上开发。
- 每项工作先从最新的 `main` 创建一个独立 branch；一个 branch 只围绕一项可以说明的工作。
- 在 branch 上修改、测试和提交；按功能检查暂存内容后再提交，允许保留清楚标注的开发 checkpoint。
- 修复完成并通过相应验证后，再把 branch 合并回 `main`。
- `main` 始终保持可测试、可构建；不使用 force push。
- 未经当前任务明确授权，不合并、不发布、不修改 remote，也不改变仓库可见性。

## Debug 与验收

- 发现 bug 时记录复现步骤、预期结果和实际结果；修复后记录验证结果。
- 独立功能或修复分别提交，便于回溯、比较和撤销。
- 浏览器测试、构建、lint、打包和已安装桌面版验收分别报告；不要把其中一种证据当成全部验收。
- 修复完成后，需要时在 GitHub issue 或提交说明中留下简明的复现和修复上下文。
- 不得运行付费 AI 服务作测试，除非当前任务明确授权并说明费用和数据边界。
